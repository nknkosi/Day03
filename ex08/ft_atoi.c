#include <unistd.h>

int     ft_atoi(char *str)
{
	int s;
	int negativ;
	int number;

	s = 0;
	neative = 0;
	number = 0;
	while ((str[s] == ' ') || (str[s] == '\t') || (str[s] == '\n') || (str[s] == '\v') || (str[s] == '\f') || (str[s] == '\r'))
		s++;
	if (str[s] == 45)
		negativ = 1;
	i

			
						
